{% extends 'base.html.twig' %}

{% block title %}Hello FoodController!{% endblock %}

{% block body %}
    <p>
        <a class="btn btn-primary" href="{{ path('food_add') }}">Ajouter une recette</a>
    </p>

    <table class="table table-bordered">
        <thead>
        <tr>
            <th scope="col">Nom</th>
            <th scope="col">Vue</th>
            <th scope="col">Actions</th>
        </tr>
        </thead>
        <tbody>
            {% for food in foods %}
                <tr>
                    <td>{{ food.name }}</td>
                    <td>{{ food.views|length }}</td>
                    <td colspan="2">
                        <p>
                            <a class="btn btn-info" href="{{ path('food_show', {'id': food.id}) }}">voir</a>
                            <a class="btn btn-danger" href="{{ path('food_delete', {'id': food.id}) }}">supprimer</a>
                        </p>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
