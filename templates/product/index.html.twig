{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1>Product index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td><a target="_blank" href="{{ product.link }}">{{ product.name }}</a></td>
                <td>{{ product.description }}</td>
                <td>{{ product.price }} €</td>
                <td>
                    <a class="btn btn-info" href="{{ path('product_show', {'id': product.id}) }}">show</a>
                    <a class="btn btn-info" href="{{ path('product_edit', {'id': product.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('product_new') }}">Create new</a>
{% endblock %}
